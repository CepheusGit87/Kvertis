<?xml version="1.0" encoding="utf-8"?>
<Page
  x:Class="Kvertis.App.Views.LicensesPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:helpers="using:Kvertis.App.Helpers"
  xmlns:services="using:Kvertis.App.Services">

  <Grid Padding="24,8,24,24" RowSpacing="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock x:Uid="Licenses_Title" Grid.Row="0" Style="{StaticResource TitleTextBlockStyle}" />
    <StackPanel Grid.Row="1" Spacing="4">
      <TextBlock x:Uid="Licenses_Intro" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" />
      <TextBlock x:Uid="Licenses_Ffmpeg_Note" Style="{StaticResource SecondaryCaptionTextStyle}" />
      <TextBlock x:Uid="Licenses_Empty_Text" Style="{StaticResource SecondaryCaptionTextStyle}" Visibility="{x:Bind helpers:Ui.Show(ViewModel.IsEmpty), Mode=OneWay}" />
    </StackPanel>

    <Grid Grid.Row="2" ColumnSpacing="16">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="240" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <ListView
        x:Uid="Licenses_List"
        Grid.Column="0"
        ItemsSource="{x:Bind ViewModel.Items}"
        SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}"
        SelectionMode="Single">
        <ListView.ItemTemplate>
          <DataTemplate x:DataType="services:ThirdPartyLicense">
            <TextBlock Text="{x:Bind Name}" TextTrimming="CharacterEllipsis" />
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      <Border Grid.Column="1" Padding="0" Style="{StaticResource CardBorderStyle}">
        <ScrollViewer x:Uid="Licenses_Text_Scroller" Padding="16" IsTabStop="True">
          <TextBlock
            FontFamily="Consolas"
            IsTextSelectionEnabled="True"
            Text="{x:Bind ViewModel.SelectedText, Mode=OneWay}"
            TextWrapping="Wrap" />
        </ScrollViewer>
      </Border>
    </Grid>
  </Grid>
</Page>
