using Microsoft.UI.Xaml.Controls;

namespace Kvertis.App.Views;

/// <summary>Step 2 "Ziel". Placeholder page; the step exists so the frame and the step header are complete.</summary>
public sealed partial class TargetPage : Page
{
    public TargetPage()
    {
        InitializeComponent();
    }
}
